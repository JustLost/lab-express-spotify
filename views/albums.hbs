    {{#each albums}}
        <h1>Albums for: </h1>
    <img src="{{this.images.0.url}}" alt="{{name}}" />
    <h2>{{this.name}}</h2>
    <button><a href="/albums/{{this.id}}">view Tracks</a></button>
    {{/each}}