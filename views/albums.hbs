<div class="container">
    
    {{#each albums}}

    <div class="artist-box">
    
    <img src="{{this.images.0.url}}" alt="{{name}}" />
    <h2>{{this.name}}</h2>
    <button class="view-button"><a href="/view-tracks/{{this.id}}">View tracks</a></button>
    </div>
    {{/each}}
</div>